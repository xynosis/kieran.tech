module.exports = {
  _name: "detach",
  _v: 1,
  methods: {
    $detach: function(el) {
      var parent, sibling;
      if (el == null) {
        el = this;
      }
      if ((parent = el.parentElement) != null) {
        sibling = el.nextSibling;
        parent.removeChild(el);
        return function() {
          return parent.insertBefore(el, sibling);
        };
      }
      return function() {};
    }
  }
};

;
