var isFunction;

isFunction = require("./_helpers").isFunction;

module.exports = {
  _name: "c-if",
  _v: 1,
  mixins: [require("./structure"), require("./if")],
  _elLookup: {
    cIf: {
      extract: {
        "": ["true", "false", "template", "else", "not"]
      },
      cb: function(o, arg) {
        var children, comment;
        children = arg.children;
        comment = document.createComment("c-if");
        if (o != null) {
          this.$nextTick(function() {
            return this.$if({
              value: o["true"] || o["false"],
              anchor: comment,
              els: children,
              template: o.template,
              elseTemplate: o["else"],
              not: o.not || o["false"]
            });
          });
        }
        return {
          el: comment,
          options: null
        };
      }
    }
  }
};

;
